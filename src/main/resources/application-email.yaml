spring:
  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:465}
    username: ${SPRING_MAIL_USERNAME:subhanvlili17@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:dxgnxqyyayharvjo}
    properties:
      mail:
        smtp:
          auth: true
          connection timeout: 5000  # 5 saniyə ərzində qoşulma uğursuz olarsa, xəta atır
          timeout: 5000            # 5 saniyə ərzində oxuma cavabı gəlməzsə, xəta atır
          write timeout: 5000       # 5 saniyə ərzində yazma bitməzsə, xəta atır
          starttls:
            enable: false
          ssl:
            enable: true
            trust: smtp.gmail.com
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false